<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>TC-S Network Presents — Prompt a Movie | Business</title>
  <meta name="description" content="Prompt a Movie turns one text prompt into a complete, production-ready film package: treatment, roadmap, screenplay, continuity-locked video prompts, rendering, and assembly." />
  <style>
    :root{
      --bg:#07070b;
      --card:#0f1017;
      --text:#e9ecff;
      --muted:#b8bdd9;
      --line:rgba(255,255,255,.12);
      --accent:#9bffb4;
      --accent2:#7aa7ff;
      --shadow: 0 20px 60px rgba(0,0,0,.55);
      --radius:18px;
      --max: 980px;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
      background: radial-gradient(1000px 600px at 20% 10%, rgba(122,167,255,.15), transparent 60%),
                  radial-gradient(900px 700px at 80% 30%, rgba(155,255,180,.12), transparent 55%),
                  radial-gradient(800px 800px at 40% 90%, rgba(255,255,255,.06), transparent 60%),
                  var(--bg);
      color:var(--text);
      line-height:1.5;
    }
    a{color:inherit}
    .wrap{max-width:var(--max); margin:0 auto; padding:38px 18px 56px}
    header{
      padding:18px 18px 8px;
      border:1px solid var(--line);
      background: linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.02));
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      position:relative;
      overflow:hidden;
    }
    header:before{
      content:"";
      position:absolute; inset:-2px;
      background: linear-gradient(120deg, rgba(155,255,180,.18), rgba(122,167,255,.15), rgba(255,255,255,.10));
      filter: blur(26px);
      opacity:.55;
      z-index:0;
    }
    header > *{position:relative; z-index:1}
    .kicker{
      display:inline-flex; gap:10px; align-items:center;
      font-size:12px; letter-spacing:.18em; text-transform:uppercase;
      color:var(--muted);
      flex-wrap:wrap;
    }
    .pill{
      padding:6px 10px;
      border:1px solid var(--line);
      border-radius:999px;
      background: rgba(0,0,0,.25);
    }
    h1{
      margin:12px 0 8px;
      font-size: clamp(30px, 4vw, 44px);
      line-height:1.1;
    }
    .sub{
      margin:0 0 18px;
      color:var(--muted);
      font-size: 16px;
      max-width: 78ch;
    }
    .ctaRow{
      display:flex;
      gap:12px;
      flex-wrap:wrap;
      margin: 14px 0 10px;
    }
    .btn{
      display:inline-flex; align-items:center; justify-content:center;
      padding:12px 14px;
      border-radius: 12px;
      border: 1px solid var(--line);
      text-decoration:none;
      font-weight: 650;
      background: rgba(0,0,0,.25);
      transition: transform .12s ease, border-color .12s ease, background .12s ease;
      min-width: 210px;
    }
    .btn:hover{transform: translateY(-1px); border-color: rgba(255,255,255,.25); background: rgba(0,0,0,.35)}
    .btn.primary{
      border-color: rgba(155,255,180,.35);
      background: linear-gradient(180deg, rgba(155,255,180,.18), rgba(0,0,0,.22));
    }
    .btn.secondary{
      border-color: rgba(122,167,255,.35);
      background: linear-gradient(180deg, rgba(122,167,255,.16), rgba(0,0,0,.22));
    }
    .btn.tertiary{
      border-color: rgba(255,255,255,.28);
      background: linear-gradient(180deg, rgba(255,255,255,.10), rgba(0,0,0,.22));
    }
    .smallNote{color:var(--muted); font-size:13px; margin: 0 0 6px}
    main{margin-top:18px}
    .grid{
      display:grid;
      grid-template-columns: 1.2fr .8fr;
      gap:14px;
    }
    @media (max-width: 860px){
      .grid{grid-template-columns: 1fr}
      .btn{min-width: 100%}
    }
    .card{
      border:1px solid var(--line);
      background: rgba(255,255,255,.03);
      border-radius: var(--radius);
      padding:18px;
      box-shadow: 0 16px 40px rgba(0,0,0,.35);
    }
    h2{margin:0 0 10px; font-size:18px}
    ul{margin:10px 0 0 18px; padding:0}
    li{margin: 8px 0; color: var(--text)}
    .muted{color:var(--muted)}
    .tagRow{display:flex; flex-wrap:wrap; gap:8px; margin-top:10px}
    .tag{
      font-size:12px;
      padding:6px 10px;
      border-radius:999px;
      border:1px solid var(--line);
      background: rgba(0,0,0,.22);
      color: var(--muted);
    }
    .hl{color: var(--accent)}
    .hl2{color: var(--accent2)}
    .mono{font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace}
    .divider{height:1px; background: var(--line); margin: 14px 0}
    .twoCol{
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap:12px;
    }
    @media (max-width: 560px){ .twoCol{grid-template-columns:1fr} }
    .offer{
      border:1px solid var(--line);
      border-radius:14px;
      padding:14px;
      background: rgba(0,0,0,.22);
    }
    .offer h3{margin:0 0 6px; font-size:16px}
    .offer .desc{color:var(--muted); font-size:13px; margin:6px 0 0}
    footer{
      margin-top:16px;
      color:var(--muted);
      font-size:12px;
      text-align:center;
      opacity:.9;
    }
    .form{
      display:grid;
      gap:10px;
      margin-top:10px;
    }
    label{font-size:12px; color:var(--muted)}
    input, textarea{
      width:100%;
      padding:12px 12px;
      border-radius:12px;
      border:1px solid var(--line);
      background: rgba(0,0,0,.25);
      color: var(--text);
      outline:none;
    }
    textarea{min-height:120px; resize:vertical}
    .mini{
      font-size:12px;
      color: var(--muted);
    }
    .copyRow{
      display:flex; gap:10px; flex-wrap:wrap;
      margin-top:10px;
    }
    .copyBtn{
      border:1px solid var(--line);
      border-radius:12px;
      background: rgba(0,0,0,.25);
      padding:10px 12px;
      cursor:pointer;
      color: var(--text);
      font-weight:650;
    }
    .copyBtn:hover{border-color: rgba(255,255,255,.25); background: rgba(0,0,0,.35)}
    .toast{
      display:none;
      margin-top:10px;
      padding:10px 12px;
      border-radius:12px;
      border:1px solid rgba(155,255,180,.35);
      background: rgba(155,255,180,.08);
      color: var(--text);
      font-size:13px;
    }
  </style>
</head>

<body>
  <div class="wrap">
    <header>
      <div class="kicker">
        <span class="pill">TC-S Network Presents</span>
        <span class="pill">Business</span>
        <span class="pill mono">Prompt a Movie</span>
      </div>

      <h1>Prompt in. Finished cut out.</h1>
      <p class="sub">
        <span class="hl">Prompt a Movie</span> turns one text prompt into a complete, production-ready film package:
        treatment → production roadmap → Fountain screenplay → continuity-locked video prompts (Sora / Runway / Pika) →
        rendering + FFmpeg assembly into a finished film with titles, credits, and optional music.
      </p>

      <div class="ctaRow">
        <!-- Replace email if you want -->
        <a class="btn primary" href="mailto:hello@thecurrentsee.org?subject=Prompt%20a%20Movie%20—%20Talk%20Business&body=Hi%20TC-S%20Network%2C%0A%0AI%20want%20to%20talk%20business%20about%20Prompt%20a%20Movie.%0A%0AProject%20type%3A%20%0ATarget%20length%3A%20%0ATimeline%3A%20%0AReferences%20or%20links%3A%20%0A%0AThanks%2C%0A">
          Talk business (email)
        </a>

        <a class="btn secondary" href="#intake">
          Quick intake (2 minutes)
        </a>

        <a class="btn tertiary" href="#usecases">
          What we make
        </a>
      </div>

      <p class="smallNote">
        This page is for creators, producers, agencies, labels, and investors. For samples, return to <span class="mono">NOW SHOWING</span>.
      </p>
    </header>

    <main class="grid">
      <section class="card">
        <h2>What the system produces</h2>
        <ul>
          <li><b>Finished cut</b> assembled from AI-rendered clips, including titles + credits</li>
          <li><b>Production package</b>: treatment, roadmap, casting profiles, style bible, and Fountain screenplay</li>
          <li><b>Clip prompt pack</b>: continuity-locked prompts with @characteridentity blocks + shot tokens</li>
          <li><b>Versioned builds</b>: restoreable snapshots for iteration, extensions, or alternate cuts</li>
          <li><b>Pipeline telemetry</b>: usage metering, retry logs, elapsed time, and build diagnostics</li>
        </ul>

        <div class="divider"></div>

        <h2>What makes it different</h2>
        <p class="muted">
          This isn’t a single AI call. It’s a <b>studio-grade agent matrix</b> that behaves like a film production workflow:
          Story → Planning → Casting → Script → Continuity Enforcement → Rendering → Assembly.
          Continuity is enforced via canonical casting profiles, contradiction checks, and lock signatures.
        </p>

        <div class="tagRow">
          <span class="tag">MCP @character protocol</span>
          <span class="tag">Continuity audits + contradiction repair</span>
          <span class="tag">Fountain screenplay output</span>
          <span class="tag">Multi-platform prompt adapters</span>
          <span class="tag">FFmpeg build system</span>
          <span class="tag">Version restore / download</span>
          <span class="tag">Usage metering</span>
          <span class="tag">Workspace telemetry</span>
        </div>

        <div class="divider"></div>

        <h2>How it works (pipeline)</h2>
        <ol class="muted" style="margin:10px 0 0 18px">
          <li><b>Treatment</b> (story + tone + arcs)</li>
          <li><b>Production Roadmap</b> (scene breakdowns, beat maps, style bible, continuity tokens)</li>
          <li><b>Central Casting</b> (canonical character profiles + identity enforcement)</li>
          <li><b>Fountain Screenplay</b> (script aligned to roadmap)</li>
          <li><b>Video Prompts</b> (continuity-locked prompts for Sora/Runway/Pika)</li>
          <li><b>Rendering & Assembly</b> (parallel clip rendering + FFmpeg assembly)</li>
        </ol>
      </section>

      <aside class="card" id="usecases">
        <h2>What we make (use cases)</h2>

        <div class="twoCol">
          <div class="offer">
            <h3>Music videos</h3>
            <div class="desc">Song upload, transcription, lyric timing, role assignments, and automatic audio overlay.</div>
          </div>

          <div class="offer">
            <h3>Short films</h3>
            <div class="desc">Narrative shorts and pilots with continuity-locked characters and style bibles.</div>
          </div>

          <div class="offer">
            <h3>Brand / concept films</h3>
            <div class="desc">Fast iteration on style and story with exportable prompt packs and build snapshots.</div>
          </div>

          <div class="offer">
            <h3>Feature-length benchmarks</h3>
            <div class="desc">Long-form validation runs with full build logs and behind-the-scenes breakdowns.</div>
          </div>
        </div>

        <div class="divider"></div>

        <h2>Interested in licensing / partnership?</h2>
        <p class="muted">
          We’re open to partnerships with studios, agencies, labels, and platform teams interested in
          long-form AI production workflows, continuity enforcement, and measurable cost/time telemetry.
        </p>

        <div class="divider"></div>

        <h2 id="intake">Quick intake</h2>
        <p class="muted">Fill this in and click send. It opens an email draft with the details.</p>

        <div class="form">
          <div>
            <label for="ptype">Project type</label>
            <input id="ptype" placeholder="Music video / Short / Pilot / Concept film / Feature benchmark" />
          </div>

          <div>
            <label for="length">Target length</label>
            <input id="length" placeholder="e.g., 60–180 sec, 5–15 min, 30 min, 90 min" />
          </div>

          <div>
            <label for="timeline">Timeline</label>
            <input id="timeline" placeholder="e.g., ASAP, 2 weeks, festival date" />
          </div>

          <div>
            <label for="brief">One-paragraph brief</label>
            <textarea id="brief" placeholder="What are we making? Any style constraints? Any must-haves?"></textarea>
          </div>

          <button class="copyBtn" id="sendEmail">Open email with this brief</button>
          <div class="toast" id="toast">Draft email opened.</div>

          <div class="copyRow">
            <button class="copyBtn" id="copyPitch">Copy 5-line pitch</button>
            <button class="copyBtn" id="copyTech">Copy technical summary</button>
          </div>

          <p class="mini">
            Tip: include a Vimeo link from NOW SHOWING so we can calibrate quality + style quickly.
          </p>
        </div>
      </aside>
    </main>

    <footer>
      © <span id="yr"></span> TC-S Network • Prompt a Movie
    </footer>
  </div>

  <script>
    document.getElementById("yr").textContent = new Date().getFullYear();

    const pitch5 = [
      "Prompt a Movie turns one prompt into a complete film package: treatment → roadmap → Fountain screenplay → continuity-locked clip prompts → render + FFmpeg assembly.",
      "It supports feature films, shorts, documentaries, and music videos, with versioned builds and usage metering.",
      "Continuity is enforced via canonical casting profiles, contradiction checks, and lock signatures in every prompt.",
      "We produce finished cuts and can also discuss licensing / partnership for teams building AI production workflows.",
      "Email: hello@thecurrentsee.org"
    ].join("\\n\\n");

    const techSummary = [
      "Technical summary:",
      "- Agent matrix pipeline (Story, Planning, Casting, Script, Dialogue/Timing, Continuity Audit, Inspector, Assembly, Telemetry).",
      "- MCP @characteridentity injection with SHA-256 lock signatures and contradiction repair.",
      "- Fountain screenplay output + platform-ready prompt packs (Sora/Runway/Pika adapters).",
      "- Rendering orchestration + FFmpeg assembly + restoreable snapshots + usage metering."
    ].join("\\n");

    async function copyText(txt){
      try{ await navigator.clipboard.writeText(txt); return true; }
      catch(e){
        const ta = document.createElement("textarea");
        ta.value = txt;
        document.body.appendChild(ta);
        ta.select();
        document.execCommand("copy");
        document.body.removeChild(ta);
        return true;
      }
    }

    function toast(msg){
      const t = document.getElementById("toast");
      t.style.display = "block";
      t.textContent = msg;
      setTimeout(()=> t.style.display="none", 2400);
    }

    document.getElementById("copyPitch").addEventListener("click", async () => {
      await copyText(pitch5);
      toast("Copied pitch to clipboard.");
    });

    document.getElementById("copyTech").addEventListener("click", async () => {
      await copyText(techSummary);
      toast("Copied technical summary to clipboard.");
    });

    document.getElementById("sendEmail").addEventListener("click", () => {
      const ptype = document.getElementById("ptype").value.trim();
      const length = document.getElementById("length").value.trim();
      const timeline = document.getElementById("timeline").value.trim();
      const brief = document.getElementById("brief").value.trim();

      const subject = encodeURIComponent("Prompt a Movie — Business Inquiry");
      const body = encodeURIComponent(
        "Hi TC-S Network,\\n\\n" +
        "I want to talk business about Prompt a Movie.\\n\\n" +
        "Project type: " + (ptype || "[fill]") + "\\n" +
        "Target length: " + (length || "[fill]") + "\\n" +
        "Timeline: " + (timeline || "[fill]") + "\\n\\n" +
        "Brief:\\n" + (brief || "[fill]") + "\\n\\n" +
        "Links/references:\\n- \\n\\n" +
        "Thanks,\\n"
      );

      window.location.href = "mailto:hello@thecurrentsee.org?subject=" + subject + "&body=" + body;
      toast("Draft email opened.");
    });
  </script>
</body>
</html>